<circle cx="${origin.x}"
        cy="${origin.y}"
        r="${radius}"
        stroke-width="${strokeWidth}"
        <#if strokeColor??>stroke="${strokeColor}"</#if>
        <#if fillColor??>fill="${fillColor}"</#if>/>