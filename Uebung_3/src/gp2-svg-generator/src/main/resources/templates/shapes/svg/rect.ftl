<rect x="${origin.x}"
      y="${origin.y}"
      width="${width}"
      height="${height}"
      stroke-width="${strokeWidth}"
      <#if strokeColor??>stroke="${strokeColor}"</#if>
      <#if fillColor??>fill="${fillColor}"</#if>/>